const { baseURL } = require('./env');

module.exports = {
  loginPage: `${baseURL}/`,
  inventoryPage: `${baseURL}/inventory.html`,
  cartPage: `${baseURL}/cart.html`,
  checkoutPage: `${baseURL}/checkout-step-one.html`,
};